<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Каталог" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
    <script src="jquery-1.7.2.min.js"></script>
  </head>
  <body>

  <%= javascript_include_tag "application" %>
  <div class="span3">
    <div class="well sidebar-nav">
      <ul class="nav nav-list">
        <li class="nav-header">Каталог</li>
        <script type="text/javascript">
            $(document).ready(function(){

                $("li").click(function(){$("#catalog").slideToggle(400);});

            });

        </script>

        <ul id="catalog">
          <% @categories.each do |category| %>
          <% if category.parent %>
                  <% then %>
                  <li><%= link_to category.title, category_posts_path(category.id), :class=>'btn btn-large btn-primary disabled'%></li>
              <%end%>
              <%end%>

        </ul>







        <% @categories.each do |category| %>
        <% if category.parent_id==nil  %>
        <%then %>
        <li><a href="#"><%=link_to category.title, category_posts_path(category.id)%></a></li>
         <%end%>
         <%end%>
        <li><a href="#">Link</a></li>
        <li><a href="#">Link</a></li>
        <li><a href="#">Link</a></li>
        <li><a href="#">Link</a></li>
      </ul>
    </div><!--/.well -->
  </div>
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">

          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">Catalog</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <li> <%= link_to "Home",categories_path %></li>
              <li><%= link_to "Contacts",new_contact_path %></li>
              <li><%= link_to "My posts",posts_path %></li>
            </ul>
            <ul class="nav pull-right">
              <li><%= link_to "Sign In",new_user_session_path %></li>
              <li><%= link_to "Выход",destroy_user_session_path, :method => :delete%></li>
            </ul>


          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
           <div class="span9">
              <%= yield %>
            </div>


        </div><!--/row-->
      </div><!--/content-->


      <footer>
        <p>&copy; Company кривые руки 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
  </body>
</html>
